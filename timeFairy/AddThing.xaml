<Window x:Class="timeFairy.AddThing"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:timeFairy"
        mc:Ignorable="d"
        Title="新事件" Height="300" Width="300">
    <StackPanel Margin="10" Width="250">
        <StackPanel.Resources>
            <Style TargetType="HeaderedContentControl">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="HeaderedContentControl">
                            <DockPanel LastChildFill="False">
                                <ContentPresenter ContentSource="Header" DockPanel.Dock="Left" 
                                  Focusable="False" VerticalAlignment="Center"/>
                                <ContentPresenter ContentSource="Content" Margin="5,0,0,0" 
                                  DockPanel.Dock="Right" VerticalAlignment="Center"/>
                            </DockPanel>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Width" Value="100"/>
                <Setter Property="Margin" Value="10,15,15,15"/>

            </Style>
        </StackPanel.Resources>

        <HeaderedContentControl Header="名称">
            <TextBox Name="name" Width="150" Text="{Binding Path=Name, Mode=TwoWay}"/>
        </HeaderedContentControl>
        <HeaderedContentControl Header="优先级">
            <TextBox Name="priority" Width="150" Text="{Binding Path=Priority, StringFormat=d, Mode=TwoWay}"/>
        </HeaderedContentControl>
        <HeaderedContentControl Header="种类">
            <TextBox Width="150" Text="{Binding Path=Kind, StringFormat=d, Mode=TwoWay}"/>
        </HeaderedContentControl>
        <HeaderedContentControl Header="开始时间">
            <TextBox Width="150" Text="{Binding Path=StartTime, Mode=TwoWay}"/>
        </HeaderedContentControl>
        <HeaderedContentControl Header="结束时间">
            <TextBox Width="150" Text="{Binding Path=EndTime, StringFormat=d, Mode=TwoWay}"/>
        </HeaderedContentControl>

        <HeaderedContentControl Header="备注">
            <TextBox Width="150" Text="{Binding Path=Etc, StringFormat=d, Mode=TwoWay}"/>
        </HeaderedContentControl>
        <StackPanel Orientation="Horizontal">
            <Button IsDefault="True" Click="Submit_Click">提交</Button>
            <Button IsCancel="True" Click="Return_Click">返回</Button>
        </StackPanel>
    </StackPanel>
</Window>
